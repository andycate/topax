[project]
name = "cax"
version = "0.1.0"

[project.scripts]
cax = "cax._app:main"

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
